-# Navigation bar partial
%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{'aria-controls' => 'navbar', 'aria-expanded' => 'false', 'data-target' => '#navbar', 'data-toggle' => 'collapse', :type => 'button'}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a{href: root_path}
        .navbar-header.logo.logo-left
          LBERG
        .navbar-header.logo.logo-right
          MAP
    #navbar.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li
          = link_to t('new_place'), new_place_path
        %li
          = link_to t('index_places'), places_path
        %li
          = link_to t('about'), about_path
      %ul.nav.navbar-nav.pull-right.locale-switch
        - I18n.available_locales.each do |locale|
          = link_to locale.to_s, locale: locale.to_s      
